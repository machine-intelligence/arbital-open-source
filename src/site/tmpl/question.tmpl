{{/* question.tmpl is the main body of the question page */}}
{{define "base"}}
<!DOCTYPE html>
<html>
<head>
	{{template "style"}}
	<link href="/static/css/question.css" rel="stylesheet">
	{{template "scripts"}}
	<script src="/static/js/question.js"></script>
</head>

<body>
{{template "navbar" .User}}

<div class="bQuestion" question-id="{{.Question.Id}}">
	<p class="questionText">{{.Question.Text}}</p>
	<a href="#" class="subscribeToQuestion" {{if .Question.IsSubscribed}}style="display:none"{{end}}>Subscribe</a>
	<a href="#" class="unsubscribeToQuestion" {{if not .Question.IsSubscribed}}style="display:none"{{end}}>Unsubscribe</a>
	{{if IsAdmin}}
		<p class="admin">
			<input type="text" class="inputQuestion"></br>
			<a href="#" class="saveQuestion">Save</a>
			<a href="#" class="cancelQuestion">Cancel</a>
			asked by {{.Question.CreatorName}}
			<a href="#" class="editQuestion">Edit</a>
		</p>
	{{end}}
</div>

<div class="bPriors">
	{{range .Priors}}
		<div class="inputColumn">
			{{template "input" .}}
		</div>
	{{end}}
	{{if .Question.PriorVote}}
		<p style="font-size:15px">You voted: {{.Question.PriorVote.Value}}</p>
	{{end}}
</div>

<div class="bInput">
	{{range .Inputs}}
		{{template "input" .}}
	{{end}}
	{{if IsLoggedIn}}
		<a href="#" class="newInputLink">New Input</a>
		<form class="newInputForm">
			<textarea name="text" class="newInputTextarea"></textarea>
			<p>URL: <input type="url" name="url"/></p>
			<input type="submit" value="Submit">
			<a href="#" class="cancelNewInput">Cancel</a>
		</form>
	{{end}}
</div>

</body>
</html>
{{end}}
