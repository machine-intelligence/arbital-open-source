{{/* updates.tmpl shows updates for the logged in user */}}
{{define "base"}}
<!DOCTYPE html>
<html>
<head>
	{{template "style"}}
	{{template "scripts"}}
</head>

<body>

{{template "navbar" .User}}

<div class="updates-body-div">
	{{range .UpdatedClaims}}
		<div class="panel panel-default">
			<div class="panel-heading">
		    <a href="{{GetClaimUrl .Claim}}" class="update-claim-summary-link panel-title">{{.Claim.Summary}}</a>
		  </div>
			<div class="panel-body">
				{{range $key, $count := .Counts}}
					{{if eq $key "reply"}}
						There are {{$count}} new replies to a comment you are subscribed to.
					{{else if eq $key "topLevelComment"}}
						There are {{$count}} new top level comments to a claim you are subscribed to.
					{{else if eq $key "newInput"}}
						There are {{$count}} new inputs to a claim you are subscribed to.
					{{end}}
					<br>
				{{end}}
			</div>
		</div>
	{{else}}
		<p>No new updates</p>
	{{end}}
</div>

</body>
</html>
{{end}}
